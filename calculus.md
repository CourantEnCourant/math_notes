# MVT (Mean Value Theorem)

Let $f : [a,b] \to \mathbb{R}$ be continuous on $[a,b]$ and differentiable on $(a,b)$, with $a < b$.

Then

$$
\exists\, c \in (a,b) : \; f'(c) = \frac{f(b) - f(a)}{b - a}.
$$
